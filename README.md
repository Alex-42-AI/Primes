# Primes
A library for working with prime numbers.
Function `sieve()` returns the list of primes up to a given number.
Function `factorize()` decomposes a number into its prime factors and assigns to each how many times it's a factor.
Function `product()` is the reverse action - by taking a dictionary, it returns the number it's a decomposition of.
